version: "3"

services:
  db:
    image: mysql:5.7
    container_name: db
    volumes:
      - "mysql-data:/var/lib/mysql"
    networks:
      - back-tier

  back:
    build: ./back
    command: python app.py
    ports:
      - "9000:80"
    networks:
      - front-tier
      - back-tier

  front:
    build: ./front
    command: nodemon server.js
    volumes:
      - ./result:/app
    ports:
      - "5001:80"
      - "5858:5858"
    networks:
      - front-tier
      - back-tier

volumes:
  mysql-data:

networks:
  front-tier:
  back-tier:
